/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package softwarephase3;

import java.util.ArrayList;
import java.util.Scanner;

/**
 *
 * @author Eslam ismail
 */
public class Manager_User {
   /* User U = new User();
     ArrayList<User> Register=new ArrayList<>();
     ArrayList<User> arrname=new ArrayList<>();
    Scanner input=new Scanner(System.in);
    String name;
        String email;
        int phone;
        float age;
        String password;
         void register(){
        User U=new User();
       
       
     System.out.println("name:");
            name=input.next();
           U.setName(name);
            System.out.println("email:");
            email=input.next();
            U.setEmail(email);
            System.out.println("phone:");
            phone=input.nextInt();
           U.setPhone(phone);
            System.out.println("age:");
            age=input.nextFloat();
           U.setAge(age);
            System.out.println("password:");
            password=input.next();
            U.setPassword(password);
           Register.add(U); 
           
           
           System.out.println("\n                 *********Registration completed successfully***********\n");
           
        }
          void Log_in(){
          User U = new User();
          Register.add(U); 
       System.out.println("name:");
            name=input.next();
            System.out.println("email:");
            email=input.next();
            System.out.println("password:");
            password=input.next();
          for(int i=0;i<Register.size();i++)
           
            {
            if(name.equals(Register.get(i).getName())&&email.equals(Register.get(i).getEmail())&&password.equals(Register.get(i).getPassword()))
           
                while (true) {                    
                   
              System.out.println("1-SharePost\n2-Comment\n3-JoinGroup\n4-SendMessage\n5-newgroup\n6-Joingroup\n7-addfriend\n8-Logout\n\n");
                   int c=input.nextInt();
                   Post p = new Post();
                   Group g = new Group();
                   Friend f = new Friend();
                  /* if (c==1)
                   {p.displaypost();
                  // System.out.println(Register.);
                   }
                  if (c==5)
                   {
                    g.newgroup();
                    g.G.add(g);
                   }
                   else if (c==6)
                   {g.joingroup();}
                   
                   else if (c==7)
                   {
                      
                   for (int j=0 ; j <Register.size() -1 ; j++)
                    
                   {System.out.println( (j+1)+"_" + Register.get(j).Name);}
                   
                  f.addfrind();
                       System.out.println("Enter name of friend who you want add him\n");
                       String idf = input.next();
                       for (int x = 0 ; x<Register.size()  ;x++ )
                       {  if (idf.equals(Register.get(x).getName()) && ! idf.equals(Register.get(i).getName()) )
                       {
                           
                           f.Acceptfriend(Register.get(x).getName());
                          //Register = removeTheElement(Register , i);
                           
                       }
                         
                       } 
                   
                   }
                   else if (c==8)
                   {break;}
                }}
            
            }
      void Log_out(){
       System.out.println("1-Register\n2-Log-in\n3-Exit\n\n");
      
}*/
    
}
